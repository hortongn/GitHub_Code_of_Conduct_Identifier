<h2>Repository Sets</h2>

<p id="notice"><%= notice %></p>

<p>
  <%= button_to 'Create New Repository Set', new_repository_loader_path, method: :get %>
</p>

<p>
  Click on an exisitng set below to generate or view its codes of conduct:
</p>

<table>
  <tbody>
    <% @repository_sets.each do |repository_set| %>
      <tr>
        <td><%= link_to repository_set.name, repository_set %></td>
        <td><%= button_to 'Delete', repository_set, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

